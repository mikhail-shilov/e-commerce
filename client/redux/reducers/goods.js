const initialState = {
  goodsOnPage: 12,
  currentPage: 1,
  goodsList: [],
  test: 42
}

export default (state = initialState, action) => {
  switch (action.type) {
    default:
      return state
  }
}
